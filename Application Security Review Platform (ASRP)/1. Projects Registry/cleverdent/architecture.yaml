architecture:
  project_id: cleverdent
  style: monolith
  gateway: nginx
  authentication: jwt
  authorization: rbac
  communication: rest
  database: postgresql
  cache: redis
  queue: ""
  storage: s3
  external_services:
    - payment-gateway
    - sms-provider
